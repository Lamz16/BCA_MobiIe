<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State saat button ditekan -->
    <item android:color="@color/blue_text_color" android:state_selected="false" />
    <!-- State default saat button tidak ditekan -->
    <item android:color="@color/column" android:state_selected="true" />
</selector>
